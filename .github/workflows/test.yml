name: CI for Print Queue Management

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: 📦 Install backend dependencies
        working-directory: ./server
        run: npm install

      - name: 📦 Install frontend dependencies
        working-directory: ./client
        run: npm install

      - name: 🧪 Run backend tests
        working-directory: ./server
        run: npm test

      - name: 🧪 Run frontend tests
        working-directory: ./client
        run: npm test

      - name: 🛠 Build frontend
        working-directory: ./client
        run: npm run build

      - name: 🧹 Lint backend (optional)
        working-directory: ./server
        run: npm run lint || echo "Lint skipped or failed"

      - name: 🧹 Lint frontend (optional)
        working-directory: ./client
        run: npm run lint || echo "Lint skipped or failed"
